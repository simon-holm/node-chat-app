import React from 'react';

export const Footer = () => {
    return (
        <footer className="page-footer">
            <div>
                <p>This should be a footer</p>
            </div>
        </footer>
    );
}
